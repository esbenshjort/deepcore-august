# ProspectorDrySpellTracker — Timing Audit
Generated: 2026-09-02 08:21:10

Stated rule (audit target): first after 32 OnShift hours dry, then every 20 OnShift hours; reset on Discovery.
Constants: First=32 Repeat=20
Test: 5 days × 10h OnShift + 14h night advance; Tick only during OnShift; Reset(0); no Discovery.

## Clock semantics in code
- `Tick(nowGameHours)` compares `nowGameHours - _lastDiscoveryGameHours` and `nowGameHours - _lastEmitGameHours`.
- Comments/constants say "Calendar game-hours" / "game-hours" — not OnShift-only accumulators.
- Runner/diagnostic: Tick only while OnShift, but night advances the same absolute clock without resetting dry-for.
- Initial: `Reset(0)` sets `_lastDiscoveryGameHours = 0`, `_lastEmitGameHours = -999`.
- Single tracker instance in runner (`_prospectorDrySpell`); diagnostic creates one local instance.

## EXPECTED (if thresholds were OnShift-only hours, start 0)
- First at OnShiftAccum = 32 → during day 4 (after 30h prior shifts).
- Second at OnShiftAccum = 52 → after day 5 ends at 50 → NOT in this window.
- EXPECTED EVENT TIMES (OnShiftAccum): 32
- EXPECTED COUNT: 1

## EXPECTED (if thresholds are absolute calendar hours, start 0, 10+14 day)
- First when calendar - 0 >= 32 and OnShift tick occurs → calendar 32 (day 2).
- Then calendar >= lastEmit+20 while OnShift → 52, 72, 96.
- EXPECTED EVENT TIMES (calendar): 32, 52, 72, 96
- EXPECTED COUNT: 4

## ACTUAL
- EmitCount = 4
- Final calendar = 120  OnShiftAccum = 50
- emit#1 calendar=32  (OnShiftAccum@emit=18)
- emit#2 calendar=52  (OnShiftAccum@emit=24)
- emit#3 calendar=72.25  (OnShiftAccum@emit=30.25)
- emit#4 calendar=96.25  (OnShiftAccum@emit=40.25)

## Prior FrustrationLoop diagnostic (Reset at calendar=8)
- ACTUAL: 40, 60, 80.25, 104.25 (= 8+32, then +20 calendar gaps; day-boundary step noise on 3rd/4th)

## ROOT CAUSE
Tracker accumulates **absolute / calendar game-hours**, including time advanced during sleep.
Tick is gated to OnShift (emit only when working), but dry-for and repeat gaps use the wall calendar, so nights count toward 32/20.
That yields ~1 event/calendar-day after onset (4 in 5 days), not 1 in 50 OnShift hours.

## FIX NEEDED: YES
(If the intended rule is OnShift-only hours: accumulate dry/on-shift hours inside Tick, ignore night advances. No rebalance in this audit.)
